package tile;

import java.awt.image.BufferedImage;

public class Tile {

    public BufferedImage image; // A imagem que será carregada (opcional por enquanto)
    public boolean collision = false; // Por padrão, o tile não tem colisão (é atravessável)

}